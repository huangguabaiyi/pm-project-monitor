export type Person = { id:string; display_name:string; role_name:string; domain_id?:string; domain?:Domain; feishu_open_id?:string; email?:string; description:string; active:boolean }
export type Domain = { id:string; name:string; color:string; description:string; sort_order:number; active:boolean }
export type Definition = { id:string; name:string; domain_id:string; domain_ids:string[]; domain:Domain; domains:Domain[]; description:string; completion_criteria:string; active:boolean }
export type TemplateNode = { id:string; definition_id:string; domain_id:string; name:string; description:string; completion_criteria:string; domain:Domain; domains?:Domain[]; position:{x:number;y:number} }
export type FlowEdge = { id:string; source:string; target:string }
export type Template = { id:string; name:string; description:string; version:number; active:boolean; node_count:number; edge_count:number; nodes?:TemplateNode[]; edges?:FlowEdge[] }
export type Risk = 0|1|2
export type AIRiskName = 'normal'|'warning'|'severe'
export type AIAnalysis = { risk_level:AIRiskName; summary:string; confidence:number; delivery_forecast:{status:'on_track'|'at_risk'|'delayed'|'unknown';reason:string}; signals:{node_id?:string;node_name:string;risk_level:AIRiskName;reason:string;evidence:string[]}[]; actions:{priority:'high'|'medium'|'low';action:string;owner_hint:string}[]; missing_information:string[] }
export type RequirementNode = { id:string; definition_id?:string; name:string; domain_name:string; domain_color:string; position:{x:number;y:number}; planned_start?:string; planned_end?:string; actual_start?:string; actual_end?:string; status:string; blocked_reason:string; notes:string; owners:Person[]; risk_level:Risk; risk_reasons:string[] }
export type Requirement = { id:string; sequence_id:number; name:string; owner:Person; owner_id:string; template_id:string; template_name:string; target_version:string; meego_url?:string; requirement_url?:string; figma_url?:string; notes:string; archived:boolean; risk_level:Risk; schedule_risk_level:Risk; risk_reasons:string[]; completed_nodes:number; total_nodes:number; progress:number; current_nodes:string[]; current_node_ids?:string[]; planned_completion?:string; nodes?:RequirementNode[]; edges?:FlowEdge[]; ai_enabled:boolean; ai_risk_level?:Risk; ai_analysis?:AIAnalysis; ai_analyzed_at?:string; ai_error?:string; ai_stale:boolean }
export type Job = { id:string; name:string; job_type:string; schedule_kind:'interval'|'cron'; cron_expression?:string; timezone:string; interval_seconds:number; notification_scope?:'all'|'risk_only'; enabled:boolean; next_run_at?:string; last_run_at?:string; last_status?:string }
export type Notification = { id:string; status:string; attempt_count:number; last_error?:string; available_at?:string; sent_at?:string; created_at?:string }
export type WebhookSettings = { enabled:boolean; runtime_environment:'test'|'prod'; test_webhook_url?:string; prod_webhook_url?:string; test_configured:boolean; prod_configured:boolean; bot_keyword:string; updated_at?:string }
export type AISettings = { enabled:boolean; provider:'chatgpt_plus'|'openai_compatible'; base_url:string; api_key?:string; api_key_configured:boolean; model:string; prompt:string; include_in_feishu:boolean; auto_analyze:boolean; updated_at?:string }
export type PlusLoginStatus = { installed:boolean; authenticated:boolean; running:boolean; output:string; status_text?:string }
export type DeploymentUpdateStatus = { enabled:boolean; running:boolean; repo_path:string; script_exists:boolean; branch?:string; last_started_at?:string; last_finished_at?:string; last_exit_code?:number; last_output:string }
export type DataMaintenanceResult = { ok:boolean; preserve_settings:boolean; deleted?:Record<string,number>; imported?:Record<string,number> }
