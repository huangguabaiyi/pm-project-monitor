# 需求进展提醒机器人使用说明

> 适用对象：需求负责人、项目负责人、研发/测试负责人，以及负责机器人运行的管理员。
>
> 当前飞书多维表格：[需求进展机器人](https://mi.feishu.cn/wiki/TA6nwzFi0i4fdOkIamzcxj34nRd?fromScene=spaceOverview&table=tblQlOtlW0xmcKBE&view=vewCeVIyDY)

## 1. 这个工具解决什么问题

机器人每天读取飞书多维表格中的需求、交付节点和阻塞项，计算各端能否按统一合板时间完成，并通过飞书卡片提醒相关负责人。

核心原则：

- 一条需求在 `需求主表` 中只占一行。
- 客户端、服务端、车辆、中枢平台、嵌入式、插件、助手等参与方，都在 `进展节点表` 中分别建行。
- 同一个交付域的研发与测试通过 `交付域 + 工作类型 + 节点名称` 配套表示，不需要给每个端单独增加新列。
- 各测试角色可以有不同的计划时间，但都以 `需求主表` 中的同一个 `合板时间` 为最终截止点。
- 有阻塞时在 `阻塞项表` 单独登记，并明确责任人和计划解决时间。
- 风险等级、最晚安全 DDL、预计完成时间和通知记录由机器人计算，不要手工填写。

## 2. 最短上手流程

第一次录入一个需求时，按下面顺序操作：

1. 在 `项目配置表` 创建一条项目配置。如果已经存在同名项目配置，直接复用，不要重复创建。
2. 在 `需求主表` 创建一条需求，填写负责人、版本、合板时间等基本信息。
3. 在 `进展节点表` 为每个实际参与方建立研发、联调、测试、发布等交付节点。
4. 如有阻塞，在 `阻塞项表` 登记阻塞内容、责任人和解决时间。
5. 排期尚未确认或需求宣讲尚未完成时，先不要勾选 `需求宣讲是否完成`。
6. 准备开始通知时，同时勾选 `需求宣讲是否完成` 和 `是否允许通知`。
7. 先让管理员执行一次 `run-once --dry-run` 预览；确认无数据错误后，再执行真实推送或启动每日定时任务。

机器人只通知同时满足以下三个条件的需求：

- `需求宣讲是否完成` 已勾选；
- `是否允许通知` 已勾选；
- `是否归档` 未勾选。

## 3. 普通填写人：日常只需要维护三张表

普通项目成员主要维护：

1. `需求主表`：需求级基本信息和统一合板时间。
2. `进展节点表`：每个人的具体交付节点、负责人和 DDL。
3. `阻塞项表`：当前阻塞、责任人和解决计划。

`项目配置表` 和 `基础配置表` 通常由项目负责人或管理员低频维护；`通知记录表` 完全由机器人维护。

---

## 4. 需求主表怎么填

一条需求只创建一行。编辑过程中允许暂时存在完全空白的占位行；机器人只会记录 Warning 并跳过，不会因此阻断其他需求推送。不要把空白行勾选为允许通知。

### 4.1 需要手工填写的字段

| 字段 | 是否必填 | 填写说明 | 示例 |
| --- | --- | --- | --- |
| 需求编号 | 必填 | 项目内唯一，后续不要随意修改 | `REQ-2026-001` |
| 需求名称 | 必填 | 清晰描述需求 | `车控快捷入口优化` |
| OKR目标 | 必填 | 填写该需求归属的季度 OKR/KR；与需求名称相同时卡片只展示一次 | `2026年Q3 KR2` |
| 当前环节 | 必填 | 首次建需求时选择当前整体所处流程阶段；后续由机器人只向后自动推进 | `各端开发` |
| 项目负责人 | 必填 | **只能选择一个人**；严重风险会 @ 此人 | `张三` |
| 产品负责人 | 选填 | **最多一个人**，用于补充项目上下文 | `李四` |
| 目标版本 | 必填 | 计划合入的版本名称 | `V8.12` |
| 需求文档链接 | 选填 | 完整的 `http://` 或 `https://` 地址；填写后卡片显示“需求文档”按钮 | `https://...` |
| Meego链接 | 选填 | 完整的 `http://` 或 `https://` 地址；填写后卡片显示“Meego”按钮 | `https://...` |
| 多语言翻译链接 | 选填 | 完整的 `http://` 或 `https://` 地址；填写后卡片显示“多语言翻译”按钮 | `https://...` |
| 合板时间 | 必填 | 所有研发、测试角色共同遵守的最终合板时间 | `2026-08-18 18:00` |
| 计划上线时间 | 选填 | 指服务端上线日期，必须早于或等于合板时间；服务端上线节点未单独排期时使用此日期 | `2026-08-13` |
| 需求宣讲是否完成 | 必填复选框 | 宣讲完成后再勾选；未勾选时不通知 | 勾选 |
| 是否允许通知 | 必填复选框 | 需要机器人开始跟踪时勾选 | 勾选 |
| 是否归档 | 必填复选框 | 需求结束后勾选；勾选后停止通知 | 不勾选 |
| 项目配置关联 | 强烈建议 | 关联到 `项目配置表` 中唯一的一条项目配置 | `示例项目` |
| 需求补充说明 | 选填 | 填写项目特有背景、依赖或风险语义 | `依赖 8 月 5 日接口冻结` |
| 备注 | 选填 | 仅在没有填写需求补充说明时作为补充说明使用 | `灰度范围待确认` |

链接全部可选，留空时不展示按钮。程序会从飞书字段对象中提取真实的 `link`、`url` 或 `href`；只有展示名称、没有真实 URL 时按空值处理，不报错。非空值必须是完整的 HTTP/HTTPS URL，非法非空 URL 只隔离当前需求，其他需求继续计算和推送。卡片按钮始终使用提取到的真实 URL。日期请使用飞书的日期选择器填写，不要写成普通文本。日常按天维护即可；未填写具体时间时，机器人按 `Asia/Shanghai` 当日 `00:00` 计算，卡片只展示 `YYYY-MM-DD`。

### 4.2 不要手工修改的系统字段

以下字段由机器人回写：

- `当前风险等级`
- `风险原因`
- `预计完成时间`
- `剩余缓冲天数`
- `最近检查时间`
- `最近通知时间`

手工覆盖这些字段不会改变真实风险计算，下次运行还会被机器人重新写入。

### 4.3 当前环节可选值

当前基础配置已预置以下环节：

1. 需求撰写
2. 内部评审
3. 产品需求评审
4. 设计稿输出
5. 设计宣讲
6. 需求宣讲
7. 工作量评估排期
8. 各端开发
9. 联调
10. 提测
11. AT 测试第一轮
12. AT 测试第二轮
13. PV 测试第一轮
14. PV 测试第二轮
15. 服务端上线
16. 线上回归
17. 多语言翻译
18. 版本合入

如果项目需要增加自定义环节，例如“安全评审”，先由管理员在 `基础配置表` 增加并启用，再在需求和节点中使用相同名称。

---

## 5. 进展节点表怎么填

`进展节点表` 是日常同步最重要的表。每一个明确的“负责人 + 交付内容 + DDL”都应单独占一行，这样日报卡片才能清楚展示每个人的交付节点。

### 5.1 必填和选填字段

| 字段 | 是否必填 | 填写说明 | 示例 |
| --- | --- | --- | --- |
| 节点名称 | 必填 | 从下拉选项中选择标准流程节点 | `AT 测试第一轮` |
| 关联需求 | 必填 | 关联到 `需求主表` 中的一条需求 | `REQ-2026-001` |
| 交付域 | 必填 | 选择客户端、服务端、车辆等参与方 | `客户端` |
| 工作类型 | 必填 | 选择研发、测试、联调、发布或设计 | `测试` |
| 负责人 | 必填 | 可选择一人或多人；普通进度会真实 @ 全部负责人 | `王五、赵六` |
| 计划开始时间 | 测试节点必填；其他节点建议填写 | 用于判断是否已经到了最晚启动时间 | `2026-08-06 10:00` |
| 计划完成时间 | 选填，建议填写 | 该负责人承诺的明确 DDL；填写后完全以此时间为准 | `2026-08-08 18:00` |
| 实际完成时间 | 完成时必填 | 节点完成后填写真实完成时间 | `2026-08-08 16:30` |
| 当前状态 | 必填 | 只能使用规定状态 | `进行中` |
| 进展说明 | 建议填写 | 说明已完成内容、剩余事项和下一步 | `主流程完成，剩余异常场景` |
| 最后更新时间 | 建议填写 | 每次修改进展说明时同步更新时间 | `2026-08-07 19:00` |

注意：`工作类型=测试` 只表示这是一条测试节点，不会向多维表格自动创建 AT/PV 测试行。节点填写 `计划完成时间` 时完全采用人工排期；未填写时，机器人按计划开始时间加项目配置时长计算，项目配置为空时使用固定默认时长。当前环节及后续没有创建的 AT1、AT2、PV1、PV2、线上回归会作为只读虚拟节点参与预计完成、缓冲和延期计算，同时在卡片底部以“流程补充提醒”小字提示。任何存在 `工作类型=研发` 节点的交付域都必须具备完整测试链路；已有测试节点，或在 `基础配置表` 中启用了同名测试角色（例如 `客户端测试`、`服务端测试`、`车辆测试`）的交付域也会纳入计算。只有设计等非研发、非测试工作不会单独触发 AT/PV 虚拟排期。

### 5.2 当前状态只能使用这些值

- `未开始`
- `进行中`
- `已完成`
- `已跳过`
- `已取消`

对已经创建的节点，`已完成` 和 `已跳过` 都视为门禁通过，`已跳过`按零时长计算，`已取消`不视为通过。节点标记为 `已完成` 时应填写 `实际完成时间`。如果某一轮不需要跟踪，可以不创建节点；如果已经创建但确认不执行，应标记为 `已跳过`。已完成或已跳过节点允许不填计划日期。不要为了消除风险而虚假标记为 `已完成`。

关键门禁规则：

- 进入 PV 前，已经创建的 AT 第一轮、AT 第二轮节点必须为 `已完成` 或 `已跳过`。
- 进入服务端上线或线上回归前，已经创建的 PV 第一轮、PV 第二轮节点必须为 `已完成` 或 `已跳过`。
- 进入版本合入前，如果已经创建线上回归节点，该节点必须为 `已完成` 或 `已跳过`。
- 多语言翻译建议在合板前完成；未完成只产生 Warning，不阻塞版本合入。
- 首次建需求时需要手动填写 `当前环节`。之后机器人只向后自动推进，不会自动回退。
- 当前环节存在一条或多条并行节点时，只有这些节点全部为 `已完成` 或 `已跳过`，才会推进。
- 推进时优先选择后续已有节点中排序最靠前的未完成环节；后续已有节点全部完成时，推进到 `版本合入`。
- 当前环节完全没有对应节点时不自动推进，也不直接判定门禁失败；卡片底部显示不阻塞、不 @ 人的“流程补充提醒”，同时当前及未来缺失的五个关键环节仍以虚拟节点参与排期计算。
- 自动推进后的环节只校验已经创建的 AT、PV、线上回归节点。未创建环节表示当前项目不跟踪该环节，不等同于未通过。

流程补充提醒示例：

```text
流程补充提醒：尚未维护 PV 测试第二轮、线上回归；如项目涉及，请后续补充。
```

该提醒本身不会改变项目风险等级，也不会写入需求主表的风险原因；如果虚拟排期计算结果晚于合板或缓冲不足，仍会按真实计算结果产生排期风险。
- 普通运行会把自动推进结果写回需求主表；`--dry-run` 只在预览卡片中展示推导结果，不修改表格。

### 5.3 不要手工修改的系统字段

- `系统风险等级`
- `系统风险原因`
- `最晚安全DDL`

这些字段由机器人根据统一合板时间、后续测试周期、阻塞项和项目配置计算。

### 5.4 交付域与工作类型

当前已启用交付域：

- 平台
- 客户端
- 服务端
- 车辆
- 中枢平台
- 嵌入式
- 插件
- 助手
- 其他

当前已启用工作类型：

- 研发
- 测试
- 联调
- 发布
- 设计

不要为了新增一个参与方而在需求主表新增一列。新增参与方时，在 `基础配置表` 新增交付域，然后在 `进展节点表` 为它增加节点即可。平台公共节点（例如线上回归、版本合入）统一使用交付域 `平台`；设计相关节点使用工作类型 `设计`。

### 5.5 如何表达“研发和测试配套关系”

同一个端的研发和测试使用相同的 `交付域`，通过不同的 `工作类型` 和 `节点名称` 区分。例如：

| 节点名称 | 交付域 | 工作类型 | 负责人 | 计划完成时间 |
| --- | --- | --- | --- | --- |
| 客户端功能开发 | 客户端 | 研发 | 客户端研发负责人 | 2026-08-05 18:00 |
| 客户端联调 | 客户端 | 联调 | 客户端研发负责人 | 2026-08-07 18:00 |
| 客户端 AT 第一轮 | 客户端 | 测试 | 客户端测试负责人 | 2026-08-10 18:00 |
| 客户端 AT 第二轮 | 客户端 | 测试 | 客户端测试负责人 | 2026-08-12 18:00 |
| 客户端 PV 第一轮 | 客户端 | 测试 | 客户端测试负责人 | 2026-08-14 18:00 |
| 服务端功能开发 | 服务端 | 研发 | 服务端研发负责人 | 2026-08-04 18:00 |
| 服务端专项测试 | 服务端 | 测试 | 服务端测试负责人 | 2026-08-08 18:00 |
| 车辆功能开发 | 车辆 | 研发 | 车辆研发负责人 | 2026-08-06 18:00 |
| 车辆 AT 第一轮 | 车辆 | 测试 | 车辆测试负责人 | 2026-08-11 18:00 |

各端测试排期可以不同，不需要强行写成同一天；机器人会分别计算，但最终都与需求主表中的统一 `合板时间` 比较。

当前 `基础配置表` 中的“测试角色”用于保存可扩展的角色目录。现阶段节点中的实际测试角色，请通过 `交付域 + 工作类型=测试 + 节点名称` 表达，例如“车辆 / 测试 / 车辆 AT 第一轮”。

### 5.6 服务端上线、线上回归和多语言翻译

关键节点顺序为：

```text
PV 测试第二轮 → 服务端上线 → 线上回归 → 多语言翻译 → 版本合入
```

服务端发布需要建立一条节点：

- `节点名称`：`服务端上线`（旧的 `上线 Checklist` 仍兼容）
- `交付域`：`服务端`
- `工作类型`：`发布`
- `负责人`：实际负责上线的一人或多人
- `当前状态`：按真实状态维护

服务端上线节点的 DDL 规则：

- 节点已填写 `计划完成时间`时，以节点日期为准；
- 节点未填写 `计划完成时间`时，使用需求主表的 `计划上线时间`；
- 服务端上线必须先于或不晚于合板，`计划上线时间` 晚于 `合板时间` 会被视为数据错误；
- 不再配置“Checklist 提前天数”。

线上回归完成后再进入多语言翻译。多语言翻译未完成时仍建议尽快处理并保留链接，但它不会升级为阻塞合板的严重风险。

---

## 6. 阻塞项表怎么填

只要某件事情可能拖慢节点，就应尽早登记，不要等到已经延期才填。

| 字段 | 是否必填 | 填写说明 | 示例 |
| --- | --- | --- | --- |
| 阻塞事项 | 必填 | 一句话说明阻塞内容 | `等待服务端接口字段确认` |
| 关联需求 | 必填 | 关联到受影响需求 | `REQ-2026-001` |
| 关联节点 | 选填但推荐 | 能定位到具体节点时必须关联 | `客户端联调` |
| 责任人 | 必填 | **只能选择一个人**，负责推动解除阻塞 | `赵六` |
| 发现时间 | 必填 | 第一次确认阻塞的时间 | `2026-08-03 14:00` |
| 计划解决时间 | 必填 | 责任人承诺的解除时间 | `2026-08-04 18:00` |
| 实际解决时间 | 解决后必填 | 真实解除时间 | `2026-08-04 16:00` |
| 当前状态 | 必填 | 建议使用 `处理中`、`已解决`、`已取消` | `处理中` |
| 是否影响合板 | 必填复选框 | 会影响统一合板时必须勾选 | 勾选 |
| 处理说明 | 建议填写 | 当前处理进展、下一步和依赖 | `接口人已确认，等待文档更新` |

以下状态会被机器人视为阻塞已结束：

- `已解决`
- `已完成`
- `关闭`
- `已关闭`
- `已取消`

如果阻塞已经解除，请同时填写 `实际解决时间`，避免后续复盘缺少真实数据。

---

## 7. 项目配置表怎么填

项目配置用于描述“这个项目与通用固定规则有什么不同”。每个项目只保留一条配置，不要建立多个同名项目配置。

先建项目配置，再在需求主表的 `项目配置关联` 中选择它。

| 字段 | 是否必填 | 填写说明 | 示例 |
| --- | --- | --- | --- |
| 项目名称 | 必填 | 排期规则配置名称，用于需求主表关联；不等同于 OKR 目标 | `默认排期规则` |
| 周期计算方式 | 必填 | 只能选择 `工作日` 或 `自然日` | `工作日` |
| AT 第一轮默认天数 | 选填 | 缺少计划完成时间时使用的 AT 第一轮时长 | `4` |
| AT 第二轮默认天数 | 选填 | 缺少计划完成时间时使用的 AT 第二轮时长 | `4` |
| PV 第一轮默认天数 | 选填 | 缺少计划完成时间时使用的 PV 第一轮时长 | `3` |
| PV 第二轮默认天数 | 选填 | 缺少计划完成时间时使用的 PV 第二轮时长 | `2` |
| 线上回归默认天数 | 选填 | 缺少计划完成时间时使用的线上回归时长 | `2` |
| 可上线日期 | 选填 | 用中文星期，以逗号分隔 | `周二, 周四` |
| 上线截止时间 | 选填 | 必须使用 `HH:MM` 24 小时制 | `17:30` |
| 是否启用 LLM | 必填复选框 | 需要 AI 补充分析时勾选；不勾选也能正常推送 | 不勾选 |
| 项目补充说明 | 选填 | 只写项目特殊背景，不要用自然语言代替结构化天数 | `车辆测试需提前预约测试车` |

填写规则：

- 天数字段必须是非负整数，不能填写 `1.5`、`约三天` 等文本。
- 留空表示沿用 `固定业务规则` 中的默认值：AT1 `4`、AT2 `4`、PV1 `3`、PV2 `2`、线上回归 `2` 个工作日。
- 填 `0` 表示明确不预留该阶段时间，不等同于留空。
- 这些时长只在对应节点缺少 `计划完成时间`，或当前/未来关键环节没有建行需要生成虚拟节点时使用；显式计划完成时间始终优先。
- `可上线日期` 支持 `周一` 至 `周日`，多个日期可用逗号、顿号或空格分隔。
- `项目补充说明` 只提供上下文；结构化天数和上线规则仍以对应字段为准。
- 项目表勾选 LLM 只是项目级开关；管理员还需要在本地配置中启用 LLM 并提供有效凭据。LLM 失效时，基础规则计算和基础消息仍然继续运行。

---

## 8. 基础配置表怎么维护

基础配置表用于扩展环节、交付域、工作类型和测试角色，属于低频管理员操作。日常新增或停用配置通常只需要维护这张表，不需要修改代码。

> **重要：先补齐下拉选项**
>
> 当前表格中的部分单选字段已经创建为“单选”类型，但选项需要手动在飞书多维表格中补齐。下方给出了完整清单。配置完成后，需求和节点表才能正常使用下拉选择。

### 8.1 手动添加单选选项

在飞书多维表格中，对需要配置的列依次执行：

1. 点击列名右侧的下拉菜单。
2. 选择 `编辑字段`、`字段设置` 或类似的字段配置入口。
3. 确认字段类型为 `单选`，不要改成多选、文本或数字。
4. 在选项区域逐项新增下方清单中的值。
5. 点击保存，并回到表格确认单元格可以正常下拉选择。
6. 所有字段配置完成后，执行一次 `init-table --dry-run` 或 `run-once --dry-run` 检查。

如果你可以编辑记录但看不到“编辑字段”入口，说明当前飞书账号只有记录编辑权限，没有表结构管理权限。此时需要让多维表格拥有者或管理员代为配置字段选项。

### 8.2 各表单选字段的完整选项

#### 需求主表

`当前环节`：

```text
需求撰写
内部评审
产品需求评审
设计稿输出
设计宣讲
需求宣讲
工作量评估排期
各端开发
联调
提测
AT 测试第一轮
AT 测试第二轮
PV 测试第一轮
PV 测试第二轮
服务端上线
线上回归
多语言翻译
版本合入
```

`当前风险等级`：

```text
正常
预警
严重
```

#### 进展节点表

`节点名称`：

```text
需求撰写
内部评审
产品需求评审
设计稿输出
设计宣讲
需求宣讲
工作量评估排期
各端开发
联调
提测
AT 测试第一轮
AT 测试第二轮
PV 测试第一轮
PV 测试第二轮
服务端上线
线上回归
多语言翻译
版本合入
上线 Checklist
```

`交付域`：

```text
平台
客户端
服务端
车辆
中枢平台
嵌入式
插件
助手
其他
```

`工作类型`：

```text
研发
测试
联调
发布
设计
```

`当前状态`：

```text
未开始
进行中
已完成
已跳过
已取消
```

`系统风险等级`：

```text
正常
预警
严重
```

#### 阻塞项表

`当前状态`：

```text
未开始
处理中
已解决
已关闭
已取消
```

#### 项目配置表

`周期计算方式`：

```text
工作日
自然日
```

#### 通知记录表

`通知类型`：

```text
项目日报
严重风险
数据异常
```

`风险等级`：

```text
正常
预警
严重
```

`发送结果`：

```text
成功
失败
```

### 8.3 选项配置注意事项

- 选项文字必须与上方完全一致，包括中文、空格和“第一轮/第二轮”等后缀。
- 不要把 `正常` 改成 `普通`。风险算法可以兼容部分旧值，但表格选项统一使用 `正常`。
- 不要删除正在使用的选项。若某个环节暂时不用，保留选项即可，不要删除历史数据依赖的值。
- `系统风险等级`、`通知记录表 / 风险等级` 属于系统字段，选项需要存在，但日常不需要人工填写。
- `通知记录表` 由机器人维护，不需要手工创建通知记录。
- 新增环节或交付域时，先在 `基础配置表` 增加同名且启用的配置，再在需求节点中使用。

| 字段 | 是否必填 | 说明 |
| --- | --- | --- |
| 配置名称 | 必填 | 要新增的名称，例如 `安全评审` 或 `海外插件` |
| 配置类型 | 必填 | 选择 `环节`、`交付域`、`工作类型` 或 `测试角色` |
| 排序 | 必填 | 同一类型中的显示和流程顺序，填写整数 |
| 是否启用 | 必填 | 只有启用的配置会参与校验和计算 |
| 备注 | 选填 | 记录适用范围和维护原因 |

新增交付域示例：

| 配置名称 | 配置类型 | 排序 | 是否启用 | 备注 |
| --- | --- | --- | --- | --- |
| 海外插件 | 交付域 | 10 | 是 | 仅海外版本使用 |

新增、停用或删除交付域、工作类型、流程环节和测试角色时，直接在 `基础配置表` 增加或维护对应记录，通常不需要改代码。删除前要确认没有现有节点正在使用；如果历史数据仍会引用，建议保留记录并关闭 `是否启用`，不要直接删除。

环节顺序需要保持为：`PV 测试第二轮 → 服务端上线 → 线上回归 → 多语言翻译 → 版本合入`。其中 `服务端上线` 必须位于线上回归之前，`多语言翻译` 必须位于线上回归之后、版本合入之前。若新增环节只是普通流程节点，维护基础配置并在进展节点表使用即可；若新增环节会改变 AT/PV/线上回归门禁、版本合入条件或严重风险判断，则还需要同步更新代码规则和回归测试。

---

## 9. 通知记录表怎么使用

`通知记录表` 由机器人自动写入，普通成员不要新增、删除或修改记录。

它主要用于查看：

- 哪条需求发送过通知；
- 通知属于普通进度、严重风险还是数据错误；
- 通知了谁；
- 发送是否成功；
- 是否使用了 LLM；
- LLM 是否发生降级以及降级原因。

如果飞书机器人发送失败，优先查看 `发送结果` 和 `错误信息`，再由管理员查看本机日志。

---

## 10. 一个完整需求的填写示例

假设需求 `REQ-2026-001 / 车控快捷入口优化` 涉及客户端、服务端和车辆，统一合板时间为 `2026-08-18 18:00`。

### 第一步：项目配置表

创建 `默认排期规则`：

- 周期计算方式：`工作日`
- AT 第一轮默认天数：`4`
- AT 第二轮默认天数：`4`
- PV 第一轮默认天数：`3`
- PV 第二轮默认天数：`2`
- 线上回归默认天数：`2`
- 可上线日期：`周二, 周四`
- 上线截止时间：`17:30`
- 是否启用 LLM：先不勾选

### 第二步：需求主表

- 需求编号：`REQ-2026-001`
- 需求名称：`车控快捷入口优化`
- OKR目标：`2026年Q3 KR2`
- 当前环节：`工作量评估排期`
- 项目负责人：选择一位项目负责人
- 产品负责人：选择一位产品负责人
- 目标版本：`V8.12`
- 合板时间：`2026-08-18 18:00`
- 计划上线时间：`2026-08-13 17:30`
- 需求文档链接、Meego链接、多语言翻译链接：有地址时填写，没有则留空
- 项目配置关联：关联 `默认排期规则`
- 需求宣讲是否完成：排期确认后勾选
- 是否允许通知：准备正式跟踪时勾选
- 是否归档：不勾选

### 第三步：进展节点表

至少建立以下节点，并给每一行选择一人或多人及明确 DDL：

| 节点名称 | 交付域 | 工作类型 |
| --- | --- | --- |
| 设计稿输出 | 平台 | 设计 |
| 客户端功能开发 | 客户端 | 研发 |
| 服务端功能开发 | 服务端 | 研发 |
| 车辆功能开发 | 车辆 | 研发 |
| 客户端联调 | 客户端 | 联调 |
| 服务端联调 | 服务端 | 联调 |
| 车辆联调 | 车辆 | 联调 |
| 客户端 AT 第一轮 | 客户端 | 测试 |
| 服务端 AT 第一轮 | 服务端 | 测试 |
| 车辆 AT 第一轮 | 车辆 | 测试 |
| 客户端 AT 第二轮 | 客户端 | 测试 |
| 服务端 AT 第二轮 | 服务端 | 测试 |
| 车辆 AT 第二轮 | 车辆 | 测试 |
| 客户端 PV 第一轮 | 客户端 | 测试 |
| 服务端 PV 第一轮 | 服务端 | 测试 |
| 车辆 PV 第一轮 | 车辆 | 测试 |
| 服务端上线 | 服务端 | 发布 |
| 线上回归 | 平台 | 测试 |
| 多语言翻译 | 平台 | 发布 |
| 版本合入 | 平台 | 发布 |

如果某个交付域不参与当前需求，就不创建对应节点；如果开发过程中发现新增参与方，直接补充节点，不需要修改需求主表结构。

### 第四步：日常更新

每次日常同步前，各负责人至少更新：

1. `当前状态`
2. `进展说明`
3. `最后更新时间`
4. 节点完成后补充 `实际完成时间`
5. 出现阻塞时新增或更新 `阻塞项表`

日报卡片会展示负责人和交付 DDL，因此不要只写“正常推进”，建议写成：

> 主流程开发完成，异常场景完成 70%；剩余蓝牙断连场景，承诺 8 月 8 日 18:00 完成，当前无新增阻塞。

---

## 11. 管理员：第一次运行

以下命令都在程序目录执行：

```bash
cd "/Users/mi/Documents/VScode项目/工作小工具/需求进展提醒机器人/.worktrees/requirement-monitor"
```

### 11.1 创建本地配置

```bash
cp config.example.json config.local.json
chmod 600 config.local.json
```

`config.example.json` 已包含当前多维表格地址以及默认的工作日 `19:30` 调度时间。

打开 `config.local.json`，直接填写本机配置：

```json
{
  "runtime_environment": "test",
  "webhooks": {
    "test": "填入测试机器人 Webhook",
    "prod": null
  },
  "bot_keyword": "需求进展推送",
  "llm": {
    "enabled": false,
    "base_url": null,
    "api_key": null,
    "model": null,
    "timeout_seconds": 20
  }
}
```

`需求进展推送` 必须与飞书机器人后台的安全词完全一致；未启用安全词校验时可以留空。`config.local.json` 已被 Git 忽略，程序会将权限收紧为 `0600`。不要把真实 Webhook 或其他 token 写入 `README.md`、`使用说明.md`、`config.example.json`、Git 历史或共享脚本。

如果不启用 LLM，保持 `enabled: false` 即可，不需要设置任何环境变量。

### 11.2 检查飞书 CLI 登录状态

```bash
feishu auth status
```

认证过期时，机器人无法读取多维表格。请先重新完成飞书 CLI 登录，再继续运行。

### 11.3 预览，不发送消息

```bash
.venv/bin/requirement-monitor run-once --dry-run
```

该命令会：

- 读取真实多维表格；
- 校验字段和关联关系；
- 计算风险并在终端打印卡片 payload；
- 不写回表格；
- 不发送 Webhook；
- 不调用 LLM。

每次大批量修改表格后，建议先执行一次 dry-run。

### 11.4 手动发送一次

```bash
.venv/bin/requirement-monitor run-once
```

这会读取表格、计算风险、回写系统字段、发送卡片并记录通知结果。

---

## 12. 管理员：启动每日自动推送

默认在 `Asia/Shanghai` 时区的每个工作日 `19:30` 运行。

配置文件填写完成后直接执行：

```bash
.venv/bin/requirement-monitor start
```

查看状态：

```bash
.venv/bin/requirement-monitor status
```

修改了 Webhook、安全词、表格地址、规则文件、发送时间或 LLM 配置后，执行：

```bash
.venv/bin/requirement-monitor restart
```

生产环境只能从 Git `main` 分支运行。非 `main` 分支会在发送前拒绝 `prod`，不会调用正式 Webhook：

```bash
.venv/bin/requirement-monitor run-once --env prod --dry-run
.venv/bin/requirement-monitor run-once --env prod
.venv/bin/requirement-monitor start --env prod
.venv/bin/requirement-monitor restart --env prod
```

`--env test|prod` 可临时覆盖配置文件中的 `runtime_environment`；未指定时直接使用本地配置。测试命令读取 `webhooks.test`，生产命令读取 `webhooks.prod`，两者不会互相回退。旧环境变量仍兼容临时覆盖，但正常使用不再需要设置。

停止自动推送：

```bash
.venv/bin/requirement-monitor stop
```

查看最近日志：

```bash
.venv/bin/requirement-monitor logs
```

注意：

- Mac 必须开机，定时任务才能运行。
- 如果 Mac 在当天 `19:30` 关机或休眠，恢复后不会补发错过的日报。
- `start` 和 `restart` 会把当前本地配置中的 Webhook、安全词和可选 LLM 凭据写入本机私密运行快照。
- 修改 `config.local.json` 后必须执行 `restart`，否则后台任务仍使用旧快照。
- 可以随时执行 `stop` 终止自动推送。

---

## 13. 通知规则

### 普通进度更新

- 卡片按负责人展示每个人的节点和 DDL。
- 消息会使用真实飞书 @ 标签通知对应节点的全部负责人，不使用人名纯文本代替。
- 适用于正常进展和一般预警。

### 严重风险

- 消息会真实 @ 项目负责人，以及当前风险节点的全部负责人。
- 卡片展示预计延期、关键路径、风险族、阻塞和建议行动，不再展开独立节点七列表格。
- 同类风险合并全部 `环节`、`交付域`、负责人和最早安全 DDL；严重风险逐项展示，Warning 合并到“其他预警”，但所有风险分类都会保留。
- `剩余缓冲为负`、`预计晚于合板`、`无法容纳合板窗口`等结果性描述会收敛为“合板窗口不足”；测试周期类重复原因也会合并。
- 卡片使用一行关键路径公式展示后续 AT1、AT2、PV1、PV2 和线上回归，不再重复展示完整延期相减公式；显式计划完成时间直接展示为节点 DDL，缺失时才展示配置或默认时长项。
- 卡片接近飞书元素数或字节上限时拆成连续卡片，标题标注“第 N/M 部分”；不会拆开风险组或静默丢失风险、真实 URL 按钮和 `<at>`。
- 每个部分都有确定性的部分指纹；重试时只发送失败部分，不重复成功部分。Webhook 卡片失败时继续使用程序内的紧凑/纯文本降级规则。
- 三个链接只在已填写时显示；需求名称与 OKR 目标相同则只展示一次。

### 不发送通知的情况

- 需求宣讲未完成；
- `是否允许通知` 未勾选；
- 需求已经归档；
- 当前没有符合通知条件的有效需求；
- 数据错误导致该需求被隔离。

---

## 14. 数据错误和常见问题

### 14.1 收到“数据错误”卡片

按卡片中的 `表名`、`需求`、`字段` 和 `修改建议` 回到多维表格修正。常见原因：

- 项目负责人、节点负责人或阻塞责任人为空；
- 项目负责人、产品负责人或阻塞责任人等单人字段选择了多个人；节点负责人允许多人；
- 合板时间为空；计划完成时间允许为空，机器人会使用项目配置或固定默认时长回退计算；
- 关联需求或关联节点失效；
- 节点状态不是规定值；
- 项目配置天数填了小数或文本；
- 节点使用了基础配置中不存在或已停用的交付域/工作类型；
- 服务端上线节点既没有计划完成时间，需求主表也没有计划上线时间；
- 计划上线时间晚于合板时间。

完全空白的编辑中行不属于数据错误：程序只在本地日志记录 Warning 并跳过，不发送异常卡片，也不影响其他有效需求。

如果卡片显示 `SNAPSHOT_ERROR`，优先检查最近是否修改过字段类型。当前版本要求 `进展节点表 / 节点名称` 使用单选字段；如果线上已经改成单选，但运行的仍是旧版本程序，旧程序会把它识别为结构不一致。请确保终端使用本项目当前工作区的 `.venv/bin/requirement-monitor`，不要使用其他目录下的旧副本。

修正后先执行：

```bash
.venv/bin/requirement-monitor run-once --dry-run
```

### 14.2 飞书机器人提示关键词错误

如果 Webhook 返回业务码 `19024`，表示机器人启用了安全关键词，但运行环境中没有配置正确关键词。

在 `config.local.json` 中修正 `bot_keyword`，然后重启：

```bash
.venv/bin/requirement-monitor restart
```

### 14.3 飞书认证过期

典型表现是读取表格失败或命令退出码为 `3`。先检查：

```bash
feishu auth status
```

重新认证后，再执行 dry-run 或 restart。

### 14.4 LLM token 过期或模型不可用

不需要停止机器人。系统会自动降级：

- 固定规则继续计算；
- 风险卡片继续推送；
- LLM 降级原因写入日志和通知记录；
- 不会因为 LLM token 过期而失去底线通知能力。

### 14.5 为什么某条需求没有通知

依次检查：

1. `需求宣讲是否完成` 是否已勾选；
2. `是否允许通知` 是否已勾选；
3. `是否归档` 是否未勾选；
4. 项目负责人等必填字段是否完整；
5. 是否至少有一个有效进展节点；
6. 是否存在数据错误卡片或本机日志错误；
7. Mac 是否在计划发送时间处于开机状态。

### 14.6 查看程序退出码

- `0`：成功，或没有需要处理的数据。
- `2`：本地配置错误。
- `3`：飞书认证、表格访问或表结构错误。
- `4`：Webhook 发送失败或部分失败。
- `5`：未预期的程序或本地调度错误。

---

## 15. 日常维护建议

### 项目负责人

- 每天同步前检查所有进行中节点是否有明确 DDL。
- 发现风险时，不要只改需求主表；应更新具体节点或新增阻塞项。
- 合板时间变更时，及时修改需求主表，并确认各端 DDL 是否仍合理。
- 需求结束后勾选 `是否归档`，停止后续通知。

### 节点负责人

- 只维护自己负责节点的状态、进展说明、最后更新时间和实际完成时间。
- 预计无法按 DDL 完成时，尽快更新说明并登记阻塞，不要等到逾期。
- 进展说明应同时包含“已完成、剩余事项、承诺时间、阻塞”。

### 管理员

- 不要修改 `固定业务规则` 来适配单个项目，项目差异应写入 `项目配置表`。
- 不要在多维表格或 Git 中保存 Webhook、机器人安全词、LLM key 或飞书认证 token。
- 修改表结构后先执行 `init-table --dry-run` 检查，不要直接反复 apply。
- 定期查看 `通知记录表` 和本机日志，确认自动任务正常运行。

## 16. 常用命令速查

```bash
# 进入程序目录
cd "/Users/mi/Documents/VScode项目/工作小工具/需求进展提醒机器人/.worktrees/requirement-monitor"

# 预览数据和卡片，不写表、不发送
.venv/bin/requirement-monitor run-once --dry-run

# 手动运行并发送
.venv/bin/requirement-monitor run-once

# 启动工作日 19:30 自动运行
.venv/bin/requirement-monitor start

# 查看状态
.venv/bin/requirement-monitor status

# 配置变更后重启
.venv/bin/requirement-monitor restart

# 停止自动运行
.venv/bin/requirement-monitor stop

# 查看日志
.venv/bin/requirement-monitor logs

# 查看版本
.venv/bin/requirement-monitor version

# 检查六张表结构，不修改线上表格
.venv/bin/requirement-monitor init-table --dry-run
```

## 17. 如何推送一条测试消息

单次测试使用 `run-once`，不要使用 `start`。`start` 会安装工作日定时任务，不适合只验证一条消息。

### 17.1 先准备一条最小测试数据

在多维表格中，至少准备以下内容：

#### 需求主表

- `需求编号`：例如 `TEST-20260727-001`
- `需求名称`：例如 `需求进展推送测试`
- `OKR目标`：例如 `2026年Q3 KR测试`
- `当前环节`：选择一个已有环节
- `项目负责人`：选择一个真实飞书用户，只选一人
- `目标版本`：例如 `TEST`
- `合板时间`：填写一个未来时间
- `需求宣讲是否完成`：勾选
- `是否允许通知`：勾选
- `是否归档`：不要勾选

#### 进展节点表

建议至少新增一条节点，这样测试卡片可以验证负责人和 DDL 展示：

- `节点名称`：例如 `测试开发节点`
- `关联需求`：关联上面的测试需求
- `交付域`：选择 `客户端`、`服务端`、`平台` 或其他已启用交付域
- `工作类型`：选择 `研发`、`测试`、`联调`、`发布` 或 `设计`
- `负责人`：选择一个或多个真实飞书用户
- `计划完成时间`：填写未来时间
- `当前状态`：选择 `未开始` 或 `进行中`
- `进展说明`：例如 `这是一次机器人连通性测试`

如果只是验证机器人能否发消息，进展节点可以暂时不建；但没有节点时无法验证负责人 DDL 展示和节点风险计算。

### 17.2 配置当前终端

打开“终端”，进入程序目录：

```bash
cd "/Users/mi/Documents/VScode项目/工作小工具/需求进展提醒机器人/.worktrees/requirement-monitor"
```

第一次运行时创建本地配置：

```bash
cp config.example.json config.local.json
chmod 600 config.local.json
```

然后在 `config.local.json` 中填写测试 Webhook 和机器人安全关键词：

```json
"runtime_environment": "test",
"webhooks": {
  "test": "填入测试机器人 Webhook",
  "prod": null
},
"bot_keyword": "需求进展推送"
```

当前测试机器人需要使用已经配置好的安全关键词；如果机器人未启用关键词校验，可以将 `bot_keyword` 留空。

### 17.3 先预览，不发送

```bash
.venv/bin/requirement-monitor run-once --dry-run
```

预览命令会读取表格并在终端打印待发送内容，但：

- 不发送飞书消息；
- 不写回风险字段；
- 不写入通知记录；
- 不调用 LLM。

先确认终端输出中包含测试需求、负责人、节点 DDL，并且没有数据错误。

### 17.4 真实推送一条

确认预览无误后，在同一个终端执行：

```bash
.venv/bin/requirement-monitor run-once --env test
```

这条命令只执行一轮：读取表格、计算风险、发送卡片、回写系统字段并写入通知记录，不会安装定时任务。

普通 `run-once` 会读取本机 `.state/monitor.json`，根据项目日报日期和严重风险内容去重。即使删除飞书 `通知记录表` 中的历史记录，本机去重状态仍然存在。需要重复验收同一天、相同内容的卡片时，可以强制发送或只预览强制发送的完整 payload：

```bash
.venv/bin/requirement-monitor run-once --env test --force
.venv/bin/requirement-monitor run-once --env test --force --dry-run
```

`--force` 仅允许用于 `test` 环境的 `run-once`，只绕过项目日报和严重风险去重；它不会绕过字段与数据校验、需求宣讲完成条件、通知开关、归档状态、风险计算或异常隔离。`prod`、`start` 和 `restart` 不支持 `--force`。其中 `--force --dry-run` 不发送 Webhook，也不写通知记录或本机发送状态。

推送成功后可以检查：

1. 飞书群中是否收到机器人卡片；
2. 卡片是否 @ 了节点负责人或项目负责人；
3. `需求主表` 的系统字段是否出现最近检查时间；
4. `通知记录表` 是否新增一条成功记录。

### 17.5 测试完成后的清理

测试需求确认无误后，可以任选一种方式处理：

- 勾选 `需求主表 / 是否归档`，保留记录但停止后续通知；
- 取消勾选 `是否允许通知`，暂时保留但不再发送；
- 删除测试需求及其节点、阻塞记录。

不要把测试需求长期保持为“已宣讲 + 允许通知 + 未归档”，否则每天的定时任务会继续把它当作真实需求跟踪。
