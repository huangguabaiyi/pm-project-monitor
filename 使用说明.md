# Pulse 使用说明

## 推荐配置顺序

1. 在“人员配置”维护需求负责人和交付成员，为每人选择交付领域并填写飞书 Open ID。
2. 在“交付配置 → 交付领域”创建产品、设计、客户端、服务端、测试等分类。
3. 在“通用节点”创建评审、开发、联调、测试等动作，并填写完成标准。
4. 在“可视化模板”把节点加入画布，通过拖动和连线定义串行或并行关系。
5. 进入“需求 → 新建需求”，选择已有负责人和模板；按需填写 Meego、需求文档和 Figma 地址。
6. 在需求详情为每个节点设置预期开始、预期结束、状态和负责人。
7. 如需智能判断，在“AI 分析”选择 ChatGPT Plus 或第三方 API，开启后可在需求详情立即分析。

## AI 分析

- ChatGPT Plus：点击“登录 ChatGPT Plus”，按照页面中的设备授权提示完成一次登录；凭证保存在 Docker 命名卷中。
- 第三方 API：填写兼容 OpenAI Chat Completions 的 Base URL、API Key 和模型名。
- 系统预置了项目交付风险 Prompt，并强制返回风险等级、总结、置信度、交付预测、风险信号、建议动作和缺失信息。
- 定时自动分析只处理尚未分析或节点信息已经变化的需求；也可以在需求详情手动重新分析。
- AI 风险与计划规则分开保存，最终展示取较高等级；AI 无法把确定性的逾期或依赖冲突降级。
- 开启“带入飞书机器人”后，机器人风险卡片会附带 AI 摘要和最多三条建议动作。

## Webhook 配置

在侧边栏进入独立的“Webhook 配置”页面：

1. 分别填写测试环境和正式环境的飞书机器人 Webhook。
2. 选择当前运行环境并设置机器人安全关键词。
3. 打开“通知已启用”并保存。

完整 Webhook 密钥只在写入时提交；保存后页面和查询接口仅显示脱敏尾号。留空保存会保留原地址，使用“清除”按钮才会移除对应环境的配置。

需求创建会复制当时模板的节点和连线。之后修改通用模板不会改变已创建需求，避免进行中的交付流程被意外改写。

## 风险规则

风险计算不使用 buffer、默认耗时或合板倒推，只读取节点时间、状态和依赖：

- 关注：计划开始或结束缺失；已经到计划开始但仍未开始；节点处于受阻状态。
- 高风险：结束早于开始；超过结束仍未完成；后置开始早于前置结束；后置已经启动但前置未完成。
- 特殊缺失：如果后续节点已经排期，而任意祖先节点没有预期结束，系统会在缺失时间的前置节点上提示风险。

并行流程在汇合处会分别检查每条前置分支，只有全部前置完成，汇合节点才满足启动条件。

## 自动任务

- `risk_scan`：重新计算所有未归档需求并为风险变化生成通知。
- `outbox_delivery`：将待发送通知投递到机器人 Webhook。

通知失败会自动退避重试，达到最大次数后进入 `dead` 状态，投递记录保存在数据库中。
